# Torus3D w Three.js / Torus3D in Three.js

(PL) Prosta aplikacja z zaimplmentowanym obracającym się torusem 3D przy użyciu biblioteki Three.js.

(EN) A simple application with a rotating 3D torus implemented using the Three.js library.
